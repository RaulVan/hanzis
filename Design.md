# Hanzis 设计系统

## 设计目标与参考

服务家长、汉字初学者与中文学习者；先呈现可操作的学习工具，避免营销统计、虚构成绩、账号入口和装饰性卡片。

本轮使用 UI/UX Pro Max（设计系统、Next.js、无障碍查询）和 Build Web Apps / shadcn。推荐中的低噪声网格、清晰层级与键盘操作适用；儿童手写英文字体、社交证明和营销大区块不符合中文工具场景，不采用。

- 字帖设计参考：`design/references/worksheet-concept.png`（1536×1024，Image Gen）。
- 诗词阅读参考：`design/references/study-concept.png`（1536×1024，同一设计语言）。
- 拼音学习参考：`design/references/pinyin-concept.png`（1536×1024，教学工作台构图）。
- 图片仅为实施参考，不作为实际页面背景或可交互 UI。字格、汉字、控件均以代码渲染。

## 色彩与材质

| Token | 浅色值 | 用途 |
| --- | --- | --- |
| background | #F8F7F4 | 页面纸白背景 |
| foreground | #292C26 | 主要文字 |
| card / popover | #FFFFFF | 表单、阅读纸面 |
| primary | #B44335 | 主操作、选中导航 |
| primary-foreground | #FFFFFF | 主操作文字 |
| muted / secondary | #EFF2EB | 预览台、次要操作 |
| muted-foreground | #62685E | 次级说明，保持 AA 对比 |
| accent | #F9EDE8 | 选中条目 |
| accent-foreground | #99362B | 选中说明 |
| border / input | #D9DDD3 | 边界 |
| ring | #B44335 | 焦点 |
| destructive | #A32D2D | 错误与清空确认 |
| success | #356346 | 明确正确反馈，附文字/图标 |

颜色定义仅在 `app/globals.css`；组件使用语义 Tailwind tokens。字帖纸张恒白，用户自选打印色属于文档数据，不跟随站点主题。首发只有浅色主题，不提供无法验收的主题开关。系统深色偏好仍呈现同一高对比浅色设计。

## 字体、间距与交互

- 拼音声调字母：`--font-sans` 首位为 “Hanzis Pinyin Sans”（同一 Noto Sans SC 的 latin-ext 子集，仅覆盖 U+0100–02FF），保证 ā/ē/ī/ō/ū/ǖ 在词内使用预组字形；不要移除或调整其顺序。
- 正文/控件：本地 Noto Sans SC（如引入），系统 PingFang SC / Microsoft YaHei / sans-serif 后备；正文 16px/1.7，控件 14–16px/1.5。
- 标题/诗词：本地 Noto Serif SC（如引入），Songti SC / SimSun / serif 后备；h1 36–44px，手机 28–32px；诗文 26–32px；不以草书渲染表单控件。
- 层级：page heading → section h2 → subsection h3，不为视觉大小跳过语义层级。
- 间距：4、8、12、16、24、32、48、64px。内容宽度最多 1280px（允许 1420px 参考图匹配时使用更宽纸台），手机 20px 边距。
- header：80px，桌面文字导航；手机显示可展开菜单，子导航可换行，不挤压为横向溢出。
- 控件至少 44px 可触区域；按钮、链接等交互控件使用 2px 朱红 focus-visible outline，文本输入框与文本域聚焦时只显示单层朱红边框，不叠加外圈；图标 lucide-react 统一 1.75–2px 描边。
- radius：控件 8px，功能纸面 12px；只有纸张轻投影，不堆叠带影卡片。
- motion：150–200ms 颜色/透明度反馈，尊重 prefers-reduced-motion，避免自动循环装饰。

## 页面结构与组件约定

- 全站：Navigation → 一个 main → Footer；skip link；路由切换后焦点回正文；导航 aria-current。
- 首页保持 `/`：标题与说明 → 左侧单一设置面板 / 右侧纸张预览；手机依次设置、预览；更多设置折叠但全功能可达。
- 拼音：`/pinyin` 概览；声母/韵母/整体认读/声调/综合练习保留可直达路径；可选择的字母表、发音与例字面板；声音能力限制明确呈现。
- 笔顺：输入/常用字 → 汉字信息 → 动画、逐笔分解与练习；URL query 可分享当前汉字；加载失败有重试，不将失败呈现成空数据。
- 扩展诗词资料：沿用诗词双栏阅读，目录每页 24 条；全部/收藏切换、朝代与选集使用现有 ToggleGroup/Select，不显示来源品牌标签或来源分类。文本保持来源换行，注解折叠；未收录明确提示。长篇使用选段对话框沿用 200 字字帖上限；扩展作品自动拼音首次默认关闭，沿用“显示拼音”开关并记住选择；标明自动注音与缺读音，加载失败保留原文并可重试；不提供无法完成的长段默写。
- 诗词朗读进度：已读正文、标点与拼音累计使用 `--primary` 主题朱红，不使用背景选中色；未读文字保持原样。停止保留进度，重播从头重置，切换作品清除。设备提供字词边界时逐字词推进，否则每句实际播放结束后标红；不使用估算计时。
- 诗词操作：精选与扩展作品均可点击单字打开页内字典弹窗，可点击外部空白、关闭按钮或按 Escape 收起，恢复原字焦点与阅读位置；弹窗采用 card 背景、圆角与既有遮罩，桌面最大 36rem、最大高度 min(80dvh, 760px)，正文独立滚动，固定底栏提供“继续读诗”和“显示详细”；“显示详细”进入完整字典；扩展作品通过 `/poetry/read/?poem=…` 进入独立阅读，可刷新与分享。生成诗词字帖直接导入正文并显示预览，保留用户排版设置，长篇仍先选取不超过 200 字的片段；通用外部文字链接保留导入确认。诗词底部不再显示“查看汉字笔顺”。
- 诗词：搜索/分类 → 简洁列表 → 阅读页（原文/拼音、译文/注释、收藏、朗读、背诵练习、生成字帖）。
- 字典多来源：简编本优先展开，修订本、第三方整理与开放词库使用原生 details 分区；只有补充来源命中时自动展开可用来源。沿用 border、muted、primary 与原有文字层级，来源标题至少 44px 点击区域；原始繁体、异读、引文不改写，加载失败与未收录分开提示。
- 小游戏目录 `/games/`：导航“汉字游戏”指向此页；三张 Card（标题链接、技能与用时、说明、“开始玩”），桌面三列、手机单列。各游戏共用 `GameStars` 星级（每次提示或每 3 次失误少 1 星，至少 1 星）、study-panel 对局区、`role="status"` 文字加图标反馈、结果 Card 与本机存储失败 Alert。
- 拼音快答 `/games/pinyin-quiz/`：设置区三组 ToggleGroup（题型、声调、时间），时间含“不限时 20 题”以满足可关闭时限；对局区题面 serif 72–96px，下方 label + 单行输入（`lang="en"`、关闭自动更正与大写）+ 提交，跳过为次要按钮；限时用 `role="timer"` 文字与 primary 细条（不用原生 progress，Chromium 下显示为绿色）；新题清空并聚焦输入框，答错保留并全选；输入法组合中不提交。
- 飞花令 `/games/feihua/`：选关为 ToggleGroup 难度 + 12 个关键字大按钮（serif 36px，显示最好星级或可选句数）；诗句逐字方格，关键字 primary 加粗，待填空为虚线格、当前空 2px primary 边框、已填为 success 边框；手机七言单行不换行（36px 格）；四个候选按钮 64px 高，错选禁用并加删除线；答完显示篇名作者、“下一句”与“读全诗”，焦点依次落在首个可选候选或“下一句”。
- 汉字词语消除 `/games/hanzi-match/`：选关（ToggleGroup 难度 + 关卡按钮，未解锁按钮禁用并写明条件）→ 对局（study-panel 内 4×4 字卡，手机单格约 70px、桌面最大 28rem 宽；桌面右侧“已找到的词语”，手机置于下方）→ 结果 Card。字卡用 card 背景与 8px 圆角，选中为 2px primary 边框 + accent 背景，提示为 2px 虚线 primary 边框，已消除格保留虚线占位以免棋盘跳动；入门关字卡上方显示词内读音，同字异读的关卡不显示。反馈使用 `role="status"` 文字加图标，正确用 success、错误用 destructive，不只靠颜色。进入对局、完成和返回时焦点移到对应标题。进度只存 localStorage，存储不可用时结果页用 Alert 提示；无计时、无广告、无音频。短横屏（高 ≤500px）棋盘按视口高度缩放、反馈与按钮并排在棋盘右侧，开局滚动到棋盘区；未解决的提示再次点击只重复高亮、不再扣星。
- 字典：输入/过滤 → 分页结果 → 单字/词语/成语释义、读音及字帖/笔顺入口；显示真实内容覆盖范围。
- 学习说明与阅读目录：放在工具主体之后，使用现有 section-title、body-copy、border 与 primary token；说明桌面三列、手机单列，目录链接保持至少 44px 点击区域。诗词详情使用简洁文字面包屑。
- 数据：笔顺、字典、拼音录音均使用同源静态资源；页面显示来源与覆盖边界。本地录音已获口头授权，书面协议说明待补充。
- Card 使用完整 Header/Title/Description/Content/Footer 组合；表单用 FieldGroup/Field，选择组用 ToggleGroup，消息用 Alert，空状态用 Empty，通知用 sonner。
- 默认 npm runner；官方 `@shadcn` registry；Radix base；导入别名 `@/`；不覆盖旧组件，CLI diff 后按需合并。

## 可见文案基线

- 导航：汉字网、Hanzis、字帖生成、拼音学习、汉字笔顺、古诗词、中文字典、汉字游戏（指向 `/games/`）。
- 首页：把每一个字，写得更好。／从一笔一画开始，让汉字学习成为日常。
- 设置：练习内容、基础汉字、古诗练习、清空、字格与排版、田字格、米字格、回宫格、空白格、每行格数、拼音标注、笔顺分解、描红练习、更多设置、导出 PDF、打印字帖。
- 预览：字帖预览、A4、页码与缩放使用真实计算结果；汉字书写练习、姓名、日期。
- 诗词：读一首诗，记一段时光。／读原文、看注释，在熟悉的诗句里认识汉字。
- 页脚：认真写字，慢慢成长。／关于与帮助／隐私说明。

## 必要的参考差异

- 生成参考图中文字和声调并非教学数据源；实现采用核验内容与真实拼音。
- 参考图纸张视觉比例不严格为 A4；实现强制 210×297mm，允许纵向延续，不裁短纸张来贴图。
- 图片中的固定 100%/1 页需替换为真实缩放和页数；空状态、loading、错误、导出进度及恢复入口为功能必要延伸。
- 通用文字链接导入字帖时需显式操作确认；诗词页已点击生成的请求直接导入并可撤销，避免重复确认；清空/恢复设置有可恢复或确认机制。
- 无账号、无跟踪；收藏与配置仅保存在本机，存储不可用时允许继续使用并提示。

## 验收

对照参考与浏览器截图核对：导航、第一屏构图、中文字体层级、颜色、面板宽度与间距、字格内容与 CTA；记录必要差异。验收桌面 1536×1024 / 1440×900、手机 375×812、横屏、200% 字号、键盘流程、减少动态效果。页面无横向溢出、控件无死按钮、状态不依赖颜色、错误可恢复。
