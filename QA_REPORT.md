# Hanzis 发布验收报告

验收日期：2026-09-01
目标：可部署的纯静态发布版
环境：macOS、Node.js 20.20.2、Next.js 16.3.3、Playwright Chromium

## 发布结论

静态发布门禁通过。五个核心模块均有真实数据和可完成的交互路径；生产构建、资源完整性、单元测试、无障碍扫描、PDF 下载、桌面与手机浏览器检查和本地静态 HTTP 检查均通过。

本报告不代表已经部署到生产，也不替代物理打印机、真实中文输入法或目标托管平台的线上确认。

## 自动化证据

| 检查 | 实际结果 |
| --- | --- |
| `npm ci` | 483 个包安装成功 |
| `npm run lint` | 退出状态 0 |
| `npm run typecheck` | 退出状态 0 |
| `npm test` | 32/32 通过 |
| `npm run build` | 编译、TypeScript、48 个静态页面生成通过 |
| `npm run release:verify` | 12,644 文件、45 HTML、9,574 笔画、1,678 录音、两组 128 字典分片 |
| `npm run test:e2e` | 2/2 通过：8 个核心页面 axe WCAG A/AA、真实 PDF 下载 |
| `npm audit --registry=https://registry.npmjs.org` | 0 个已知漏洞 |
| `git diff --check` | 通过 |

构建唯一提示为 `caniuse-lite` 数据已 9 个月未更新，不影响编译或运行；本轮没有仅为消除提示而升级依赖。

## 功能验收

### 字帖生成

- 四类字格、拼音、笔顺分解、描红、颜色、行列和边距更改会同步更新预览。
- 空白内容、分数设置、关闭描红、超过 200 字等边界均显示明确状态。
- URL 导入先确认再覆盖当前设置。
- 实际 PDF 为 3 个 A4 页面，每页 595.2756×841.8898 pt；第 2 页 PNG 为 2379×3364 px。
- 打印 iframe 解码 3 张页面图片；未调用系统打印机。

### 拼音学习

- 声母、韵母、整体认读、声调和综合练习均可直达。
- 第一声到第四声显示 55、35、214、51 调值；轻声单独解释。
- `mā` 实际走本地录音并完成播放；轻声 `ma` 明确走设备系统中文语音。
- 10 题练习在选择前禁用“下一题”，回答后显示正确/错误反馈与音频来源。

### 汉字笔顺

- 查询、URL 参数、速度、播放、暂停、继续、重播和逐笔分解可用。
- 互动书写可开始、提示、退出和重试；提示只示范当前笔画，不会增加完成数。
- 不支持字符和资源失败显示可恢复错误。

### 古诗词

- 总计 30 首；宋词筛选为 10 首，李清照筛选为 2 首。
- 搜索、收藏、本地恢复、空收藏、逐字词典和字帖/笔顺入口可用。
- 背诵错误反馈、揭示答案和下一句流程可完成。

### 中文字典

- “学习”返回 4 个结果并同时展示教育部与开放释义。
- 拼音 `xue` 返回 15 个结果；不存在词显示空状态。
- 部首/笔画折叠按钮不会提交表单；无效输入显示错误。
- 数据覆盖范围、来源和公开许可入口可见。

## 浏览器与响应式

桌面视口对字帖、拼音、笔顺、诗词、字典、关于和隐私页进行了真实交互。手机视口 375×812 检查以下 12 条路径：

`/`、`/pinyin/`、`/pinyin/initials/`、`/pinyin/finals/`、`/pinyin/syllables/`、`/pinyin/tones/`、`/pinyin/practice/`、`/stroke/?char=永`、`/poetry/`、`/dictionary/?q=学习`、`/about/`、`/privacy/`。

结果：

- 每页仅一个 `main`，标题可见。
- 文档宽度等于视口宽度，无横向溢出。
- 最终字典快捷按钮修正后，所有可见按钮均至少 44×44。
- 关键状态不只依赖颜色；焦点、错误和结果均有文字说明。

## 无障碍

Playwright + axe 对首页、拼音概览、声调、笔顺、诗词列表、诗词详情、字典、关于页执行 WCAG A/AA 自动扫描，未发现自动可检测违规。

另外手动核对了 skip link、标题层级、`aria-current`、表单标签、按钮类型、错误 `role=alert`、键盘焦点和减少动态效果。自动扫描不能替代完整人工读屏验收。

## 视觉参考对照

| 对照项 | 结果 |
| --- | --- |
| 字帖页导航、标题和首屏节奏 | 与 `worksheet-concept.png` 的低噪声纸面和顶部层级一致 |
| 字帖设置/预览双栏比例 | 保留概念图主次关系；真实 A4 比例、页数和缩放替换静态占位 |
| 全站颜色、字体、边框和卡片 | 统一使用纸白、墨色、朱红、轻边框与 Noto 中文字体 |
| 诗词标题与搜索行 | 与 `study-concept.png` 的顶部构图和阅读优先层级一致 |
| 诗词列表与阅读区 | 保持左侧筛选/列表、右侧阅读内容的清晰层级，并加入真实收藏/背诵状态 |
| 拼音导航、选择区和详情区 | 与 `pinyin-concept.png` 的教学工作台构图一致；实际页面使用经核验的调值曲线和示例 |

必要差异包括：严格 A4 物理尺寸、真实空/错/载入状态、动态页数、系统语音回退、查询确认和可恢复设置。参考图片仅作为设计方向，没有作为页面背景或伪造交互。

## 静态 HTTP 与发布包

- 首页：200、`text/html`、CSP、`nosniff` 与 Referrer-Policy。
- 不存在路径：404 并返回自定义页面。
- MP3 Range：`bytes=0-31` 返回 206、`Content-Range: bytes 0-31/16926` 与 32 bytes。
- robots、sitemap、manifest：200；manifest 为 `application/manifest+json`。
- ZIP：87,471,219 bytes、12,697 个条目；`unzip -t` 无错误且根目录包含 `index.html`、`404.html`、`_headers`、主要路由和录音。
- SHA-256：`50c90461a60f77b2db95efc214d86a9f80e868b08606830083bdac4f98b805a4`。

## 已知限制与发布后检查

- 未使用物理打印机；只验证 PDF 物理尺寸、PNG 和浏览器打印 iframe。
- 未以真实中文输入法发送 composition 事件；代码路径已实现输入合成保护。
- 系统中文 TTS 音色与可用性由设备和浏览器决定。
- 生产环境已通过 GitHub `main` 自动部署到 Cloudflare Pages；正式域名、安全响应头、自定义 404、JSON 静态资源与 MP3 Range 已完成线上冒烟。Cloudflare Zone 自动注入的 Web Analytics 已在 CSP 与隐私说明中明确处理。
- 拼音录音已获口头授权；书面授权协议说明仍需归档。
