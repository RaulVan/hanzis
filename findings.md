# Hanzis 发布版关键发现

## 基线与架构

1. 原文档对拼音、笔顺、诗词和字典同时存在“完成”与“待开发”标记，无法作为发布依据。本轮改为以真实路由、交互和验证结果维护功能矩阵。
2. 当前 Next.js 16.3.3 与旧版本约定不同；实现前以本地 `node_modules/next/dist/docs/` 为准。robots、sitemap 和 manifest 在纯静态导出中显式采用静态生成。
3. 应用没有服务端运行时需求。学习数据、字形、录音、字体和页面都能由同源静态服务器提供，适合生成根目录 ZIP。
4. 根布局集中管理导航、一个 `main` 与页脚；业务页面按模块拆分，避免重复壳层和单文件持续膨胀。

## 数据与授权

1. Hanzi Writer 数据可预生成 9,574 个同源 JSON，笔顺动画和练习无需运行时访问第三方接口。
2. cnchar-data 快照含 278,369 个字词键，适合按 Unicode 首码点切成 128 个开放释义分片。
3. 教育部《國語辭典簡編本》版本 `2014_20260626` 含 45,130 条原始记录和 6,028 个单字词头；原字段保持不变，简体检索映射与学习提示在站点层独立处理。
4. 两套字典快照均由构建脚本核对 SHA-256，来源或归属改变会使构建失败，避免静默漂移。
5. `public/voice/` 的 1,678 个 MP3 已获口头授权，书面协议说明待补充。技术验收确认全部文件可解析；轻声不会错误借用一声音频，未收录音节明确回退到设备系统中文语音。
6. 30 首唐宋诗词的逐字拼音、译文与学习注释在项目内维护；测试保证每行汉字数和拼音音节数一致。

完整来源、处理、许可和校验值见 [DATA_SOURCES.md](DATA_SOURCES.md)。

## UI 与交互

1. 低噪声纸张视觉、朱红主色、本地 Noto 字体和语义 tokens 能同时服务字帖、诗词阅读与拼音学习，无需为每个模块建立独立主题。
2. 字帖参考图的纸张比例并非严格 A4；发布实现固定为 210×297mm，并用真实页数、缩放和导出状态替代概念图中的静态占位数字。
3. 手机端需要让子导航换行并保持 44px 触控区域。最终发现字典单字快捷按钮宽度只有 38px，已针对性补至 44px。
4. 字典高级筛选必须使用真实 Radix Collapsible，且触发按钮显式为 `type="button"`，否则位于表单中会意外提交查询。
5. 拼音声调不能只用符号表达；发布版显示 55、35、214、51 的真实调值曲线，并把轻声解释为短而轻、无固定调值。

## 实施中发现并修复的问题

1. 笔顺练习的“提示下一笔”原调用 `skipQuizStroke()`，会把一画字直接判定完成。改为高亮当前笔画后，提示仍停留在练习状态且完成数不增加。
2. 字帖设置必须保留合法的 0、分数滑杆值和 Unicode 扩展汉字；旧配置或未来版本配置会先提示备份，不静默覆盖。
3. 拼音练习需要确定性题目生成，才能稳定测试十题交替题型、每题唯一正确答案和全部声调覆盖。
4. 本地预览服务器最初把 `.webmanifest` 作为二进制流返回；补充 `application/manifest+json` 后，浏览器和静态 HTTP 验收一致。
5. MP3 必须支持 Range 请求；发布预览已实测 `bytes=0-31` 返回 206、正确 Content-Range 和 32 bytes。

## 安全、隐私与发布边界

1. 移除不再需要的高风险/冗余依赖后，官方 npm registry 审计为 0 个已知漏洞。
2. 静态站点发布 `_headers` 包含 CSP、nosniff、Referrer-Policy、X-Frame-Options 与 Permissions-Policy；目标托管平台是否实际应用仍需线上确认。
3. 页面不接入账号、分析或广告脚本；localStorage 仅保存字帖配置与收藏。静态托管方的访问日志属于部署配置边界。
4. 本轮只生成并校验本地发布包，没有修改域名、生产环境、远端 Git 或线上数据。

## 剩余非代码事项

- 补充并归档录音书面授权协议说明。
- 在目标生产托管环境执行 URL、缓存、安全头与大资源 Range 冒烟测试。
- 用物理打印机和真实中文输入法完成设备相关验收；现有结论只覆盖浏览器打印 iframe、PDF 尺寸和输入事件代码路径。
