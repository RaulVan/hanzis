# Hanzis 发布版实施计划

## 目标与边界

- 目标：把字帖、拼音、笔顺、诗词、字典五个模块完成为可正式发布的静态网站，并统一视觉、交互、无障碍、数据来源和部署交付。
- 基线：Next.js App Router、TypeScript、Tailwind v4、Zustand、Hanzi Writer、cnchar 与本地静态数据。
- 数据原则：构建不联网拉取学习数据；来源、版本、处理方式、许可和校验值可追踪。
- 隐私原则：无账号、无广告与分析 SDK；设置和收藏只保存在本机。
- 范围外：账号、云同步、支付、多语言、生产域名配置，以及未经明确授权的提交、推送或线上部署。
- 音频边界：现有 1,678 个 MP3 已获口头授权并纳入发布；书面授权协议说明待补充。

## 架构职责

| 层级 | 职责 |
| --- | --- |
| `app/` | 静态页面、元数据、错误边界、404、robots、sitemap 与 manifest |
| `components/layout`、`components/ui` | 全站结构、导航、页脚、shadcn/Radix 基础组件与交互语义 |
| `components/{worksheet,pinyin,stroke,poetry,dictionary}` | 各业务模块的 UI 与交互 |
| `lib/`、`stores/` | 数据适配、导出、朗读、查询、本地状态与错误边界 |
| `data/`、`public/`、`licenses/` | 版本化学习内容、发布资源、公开许可与归属 |
| `scripts/`、`tests/` | 资产生成、静态包校验、单元测试与浏览器验收 |

每个页面只渲染一个 `main`；全站导航和页脚由根布局统一提供。非生成源文件按单一职责拆分，保持在 500 行以内。

## 实施结果

| 阶段 | 结果 | 核验 |
| --- | --- | --- |
| 基线审计 | 完成 | 核对 Git 根、已有改动、文档矛盾、依赖与本地资源 |
| 设计系统 | 完成 | 语义 tokens、Noto 字体、响应式导航、键盘焦点、44px 触控区 |
| 字帖生成 | 完成 | 四类格子、拼音/笔顺、描红、颜色、持久化、多页 PDF/PNG/打印 |
| 拼音学习 | 完成 | 声母、韵母、整体认读、真实调值曲线、录音、回退语音、10 题练习 |
| 汉字笔顺 | 完成 | 本地 9,574 字、动画控制、逐笔分解、书写练习、错误恢复 |
| 古诗词 | 完成 | 30 首、搜索筛选、详情、拼音、译注、收藏、朗读、背诵、字帖联动 |
| 中文字典 | 完成 | 中/繁/拼音/部首/笔画检索、双来源释义、分页、空状态与模块联动 |
| 发布加固 | 完成 | SEO、404、安全响应头、Range、静态验证、来源与隐私页面 |
| 完整 QA | 完成 | Node 20 检查、构建、32 项单测、2 项 E2E、桌面/手机浏览器验收 |
| 发布产物 | 完成 | 根目录 ZIP、SHA-256、压缩包完整性与关键路径验证 |

## 成功标准与结论

- 五个导航入口均为真实功能，无占位页面。
- 核心交互具有明确的空、错、载入、成功与恢复状态。
- 桌面与 375×812 手机视口无横向溢出；可见按钮触控区域达到 44×44。
- PDF 为真实多页 A4 文件，PNG、打印 iframe 与静态资源路径可用。
- `npm ci`、lint、TypeScript、32 项单元测试、生产构建、静态包校验、安全审计和 2 项 Playwright 验收均通过。
- 发布包可解压到静态站点根目录，且包含安全头配置、404、SEO 文件、字典、笔画和录音资源。

项目已达到静态发布准备状态。尚未执行生产上传；物理打印机、真实中文输入法 composition 事件和生产托管平台响应头仍需在目标环境做最终冒烟确认，详见 [QA_REPORT.md](QA_REPORT.md) 与 [DEPLOYMENT.md](DEPLOYMENT.md)。
